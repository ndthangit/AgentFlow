# Development-only defaults. Copy this file to .env and replace every password.
POSTGRES_DB=agentflow
POSTGRES_USER=agentflow
POSTGRES_PASSWORD=change-agentflow-db-password

KEYCLOAK_DB=keycloak
KEYCLOAK_DB_USER=keycloak
KEYCLOAK_DB_PASSWORD=change-keycloak-db-password
KEYCLOAK_ADMIN=admin
KEYCLOAK_ADMIN_PASSWORD=change-keycloak-admin-password
KEYCLOAK_DEV_USER=developer
KEYCLOAK_DEV_USER_PASSWORD=change-developer-password

# The LLM server must be reachable from inside Docker.
# Docker Desktop: host.docker.internal; Linux: host-gateway is configured below.
AGENT_PROVIDER=openai-compatible
AGENT_MODEL=your-tool-calling-model
OPENAI_COMPATIBLE_BASE_URL=http://host.docker.internal:8000/v1
OPENAI_COMPATIBLE_API_KEY=

AGENT_DEMO=true
